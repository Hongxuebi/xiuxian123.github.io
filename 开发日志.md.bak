# 流云记 · 鸿蒙开发日志

> 项目路径：`/Users/caixiaodan/liuyunji`
> 目标：爱助手（love-helper）Web 应用嵌入鸿蒙 WebView + 原生 TTS/ASR 能力

---

## v0.7.9 — 抽屉头像区照片显示 + 各bug修复（2026-05-09）

### 背景
v0.7.8 重构智能体选择器后引入多个 bug，集中修复。

### 修复清单

**Bug 1 — 智能体选择器按钮点击无反应**
- 根因：`渲染下拉菜单` 缺少 `下拉菜单.appendChild(项)`，菜单永远为空
- 连带问题：async 无超时可永久挂起、无 try-catch 日志静默吞错
- 修复：加 appendChild、5s Promise.race 超时、console.log 日志

**Bug 2 — 下拉选择后菜单不关闭**
- 根因（两层）：① `classList.remove('显示')` 在 await 之后，切换卡住则不执行 ② CSS 缺少 `.智能体下拉菜单 { display:none }`，切 class 无可见性影响
- 修复：先关菜单再切 + CSS 补 `display:none/display:block`

**Bug 3 — 选择智能体后头像区异常**
- 根因：`更新抽屉头像` 用 `.抽屉 .智能体头像` CSS 选择器，实际元素是 `#抽屉智能体头像`，永远匹配不上
- 修复：改用 `getElementById`

**Bug 4 — 新建会话后点旧会话无法加载**
- 根因：`智能体切换` 事件监听器硬编码 `当前会话ID = 'default'`，覆盖了 `切换智能体` 的正确设置
- 修复：只有当前会话 ID 不在新列表时才兜底

**Bug 5 — 抽屉头像区点击无反应**
- 根因：`绑定头像预览` 函数在 v0.7.8 重构时整体删除，但 `初始化.js` 仍在调用
- 修复：从 git 恢复 `window.绑定头像预览` 函数（含 touchend 鸿蒙适配）
- 教训：重构删函数前必须 grep 调用点

**Bug 6 — 抽屉头像区不显示照片**
- 根因：`更新抽屉头像` 中 `icon`（emoji）优先于 `avatar`（照片），智能体同时有两者时照片被跳过
- 修复：调换分支顺序——有照片优先显示照片，没照片才用 emoji

### 代码纪律（新增硬规则）
- 写 JS 后必须 `node --check` 语法检查
- try-catch 先写闭合括号再填内容
- DOM 创建必须紧随 appendChild
- async 关键调用加超时 + try-catch 日志
- 模板字符串内有 Markdown 反引号必须转义 \`\`\`
- touchend/click 分开处理，不在 click 里乱调 preventDefault

### 文件变更
| 文件 | 变更 |
|------|------|
| `js模块/智能体选择器.js` | appendChild、超时、日志、CSS/JS 配合、头像预览恢复、照片优先级修复 |
| `js模块/对话管理.js` | 会话ID覆盖修复 |
| `js模块/初始化.js` | 调试面板清理 |
| `css/样式.css` | 下拉菜单 display:none/block |
| `js模块/API调用.js` | list_agents 加设定摘要、manage_agents 描述加
| `js模块/API调用.js` | list_agents 加设定摘要、manage_agents 描述加修改前先读约束 |

### 调试方法论
- **克隆按钮诊断法**：cloneNode+replaceChild+简单handler，快速区分事件绑定 vs 回调逻辑问题
- 下拉菜单必须 CSS + JS 配合（JS 切 class，CSS 根据 class 控制 display）
- 修复 bug 前查 git history 确认原始设计意图

### 标签
- love-helper: v6.4（push）
- liuyunji: v0.7.9（sync + push）

---

## v0.7.7 — 智能体管理与对话管理 AI 工具（2026-05-08）修改前先读"约束 |

---

## v0.7.7 — 智能体管理与对话管理 AI 工具（2026-05-08）

### 变更概要
- **智能体管理工具**：新增 `list_agents`、`create_agent`、`switch_agent`、`delete_agent` 四个 AI 可调用工具
- **对话管理工具**：新增 `list_conversations`、`create_conversation`、`switch_conversation`、`rename_conversation`、`delete_conversation` 五个 AI 可调用工具
- **系统提示词**：`智能体管理.js` 中追加完整的新工具使用指南（参数为中文名、返回值 `{success, data/error}` 格式解析、二次确认要求、联合调用示例、列表展示格式、重名处理）
- **底层函数暴露验证**：`对话管理.js` 中 5 个核心函数（`会话列表`、`新建会话`、`切换会话`、`重命名会话`、`删除会话`）全部已 `window.xxx =` 暴露

### 文件变更
| 文件 | 变更 |
|------|------|
| `js模块/API调用.js` | +351 行：新增 4+5 个工具定义、handler 逻辑、映射表、白名单 |
| `js模块/对话管理.js` | +45 行 |  
| `js模块/智能体管理.js` | +66 行：提示词章节完善 |

### 完整验证清单（代码级，未真机测试）
- ✅ 5 个对话管理底层函数全部暴露到 window
- ✅ 9 个工具定义全部在 `_备忘录工具列表` 中
- ✅ 9 个 handler 分支全部存在（try/catch，返回统一 JSON 格式）
- ✅ 9 条映射表（`const 工具映射`）全部存在
- ✅ 3 个敏感操作在工具白名单中（删除会话、删除智能体、重命名会话）
- ✅ love-helper 三文件同步，diff 一致

---

## v0.7.8 — 智能体设定注入修复（2026-05-08）

### 背景
用户修改智能体核心设定（身份、语气、规则等）后，AI 无法感知变更。
根因：设定写入 `agent.json.plugin`，但对话中使用的 `system.md` 未同步更新。

### 方案（15行最小改动，零侵入）

**修改A — 最高意志注入段（对话管理.js）**
- 位置：`获取系统提示词()` 中，2.1（身份声明）之后、2.2（运行状态）之前
- 从 `agent.json.plugin` 读取 core_identity / tone_requirement / output_rules / taboo_rules
- 以「用户为你设定的核心规则（最高意志，优先级高于一切）」强标题注入
- `_设定刚更新` 标记为 true 时附加 ⚠️ 提醒

**修改B — 设定变更标记（智能体编辑面板.js）**
- 位置：`保存配置()` 末尾、`加载智能体()` 之后
- `window._设定刚更新 = true`，10秒后自动清除

### 文件变更
| 文件 | 变更 |
|------|------|
| `js模块/对话管理.js` | +19 行：新增 2.15 最高意志注入段 |
| `js模块/智能体编辑面板.js` | +4 行：设定变更标记 |

### 验证说明
- 改完即 commit + tag v0.7.8，已推送远程
- love-helper 同步完成
- 验证场景：设置中修改核心身份为「异世界狐妖，筑基期一层」→ 同对话发「你是谁？」

---

### 审核意见修复（来自开发者的补充审核）
- 工具参数名修正为中文（`名称`、`图标`、`设定`、`智能体名称`、`会话ID` 等）
- 返回值格式新增 `{success, data/error}` 解析指引
- `delete_conversation` 增加二次确认（同 `delete_agent` 级别）
- 联合调用示例：create_agent → 取 data.id → switch_agent 分段执行
- 列表展示格式说明：含 isCurrent/📌置顶标记
- 名称不存在时主动列候选，不直接报错
- 重命名重名处理提示

---

## v0.7.6 — 导出 JSON 空文件修复（2026-05-08）

### 变更
- `saveTextFile` 桥接修复：`TextEncoder.encodeInto` 单参数 → `encoder.encode`、`WRITE_ONLY` → `WRITE_ONLY\|CREATE`、`writeSync` 传 `buffer.buffer as ArrayBuffer`
- `onAlert` 回调修复：`event.getMessage()` → `event.message`
- AI 工具定义中补全 `收藏` 和 `已置顶` 参数（`update_memo`）

---

## v0.7.5 — alert 替换为浮动提示体系（2026-05-08）

### 变更
- `初始化.js` 新增全局 `_显示提示()` 函数（顶部/居中/2.5s/三类型 info/success/error）
- 批量替换 16 个 JS 文件中的 `alert()` → `_显示提示()`
- `Index.ets` 新增 `onAlert` 兜底回调（`promptAction.showToast`）
- 样式.css 新增 `.按钮闪光` 类

---

## v0.7.4 — 使用统计注入（2026-05-08）

### 变更
- 每次对话自动注入：联网搜索次数、对话次数、备忘录浏览次数、主题切换次数、当前主题、当前会话名
- 新增对话统计点（`对话管理.js` `_收集统计`）
- 扫描发现大量残留 `alert()`（33处）

---

## v0.7.3 — AI 系统运行状态注入（2026-05-08）

### 变更
- `对话管理.js` `_收集统计` → 注入联网搜索状态+Key、当前主题、当前会话名

---

## v0.7.2 — 联网搜索提示词强化（2026-05-08）

### 变更
- `getSystemPrompt`（对话管理.js）联网搜索章节重构

---

## v0.7.1 — 操作栏按钮双击修复（2026-05-08）

### 变更
- `卡片滑动.js`、备忘录按钮增加 touchend+preventDefault，修复鸿蒙 WebView 双击生效

---

## v0.6 — 编辑器 + 无痕模式（2026-05-07）

- 编辑器支持字号、颜色、加粗 / 斜体、图片、附件上传、TODO 复选框
- 无痕模式（对话历史概不保存，切换即销毁）

---

## v0.5 — 百度搜索桥接（2026-05-07）

- `Index.ets` baiduSearch 桥接（@ohos.net.http 原生 HTTP，绕过 WebView CORS）
- `API调用.js` 检测 nativeBridge.baiduSearch

---

## v0.4 — AI 上下文注入 + 使用统计（2026-05-07）

- 系统提示词自动注入联网搜索状态、使用统计

---

## v0.3 — 卡片滑动体验完善（2026-05-07）

- 卡片滑动 v5（方向判定 8px + 竖滚早期退出 + 滑动保护中 300ms + 误触修复）
- 智能体专属文件夹注入提示词、默认权限 none

---

## v0.2 — 智能体权限体系（2026-05-07）

- 权限向导浮层、删除确认向导、智能体重命名同步文件夹、默认权限 none

---

## v0.1 — 鸿蒙应用初始化（2026-05-07）

- HarmonyOS API 22 + WebView 容器嵌入爱助手 HTML/JS/CSS
- 35 JS + 2 CSS 运行在 `resources/rawfile/`
- 下载/导出/备份桥接修复（NativeBridge.saveFile + DocumentViewPicker.save）
- alert→浮动提示体系

---

## 爱助手原项目日志

完整开发日志请见：
- `/Users/caixiaodan/Desktop/dev/love-helper/开发日志.md`（1328 行）
- `/Users/caixiaodan/Desktop/dev/love-helper/常见错误对照速查.txt`
